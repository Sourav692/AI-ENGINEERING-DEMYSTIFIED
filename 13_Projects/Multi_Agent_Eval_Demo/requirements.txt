# Everything here is already covered by the repo's own extras:
#   uv pip install -e ".[fullstack,apps]"
# This file exists so the demo can also run standalone.
langgraph>=1.2.11
langchain-core>=1.6.1
fastapi>=0.115
uvicorn>=0.32
streamlit>=1.40
requests>=2.32
pydantic>=2.9
