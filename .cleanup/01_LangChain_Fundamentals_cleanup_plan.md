---
target: 01_LangChain_Fundamentals
slug: 01_LangChain_Fundamentals
generated: 2026-09-07
status: applied
notebooks: 38
decisions: 01_LangChain_Fundamentals_cleanup_decisions.md    # may not exist yet
---

# Cleanup plan — `01_LangChain_Fundamentals`

A 7-track LangChain course (38 notebooks at planning time: `01_Getting_Started` →
`07_LangChain_1x_Agents_and_Middleware`). **Q-001 update (2026-09-07):** this folder is
`01_LangChain_Fundamentals` at the repo root, and turned out to already BE the current
Phase 1 folder — the repo's top-level phase structure was renumbered 0-16 in commit
`30ddca0`, and `CLAUDE.md`/`NOTEBOOK_INDEX.md` (describing an old 13-phase, 1-indexed
layout with a separate `02_LangChain_Fundamentals_and_Prompting/LangChain_Fundamentals/`
track) are stale relative to disk. No merge/replace was needed; Q-001 is resolved as moot.
Content is substantively complete throughout — no syntax errors, one consolidation
(NB-014 into NB-011) — and the formatting pass (title/summary cells, banner comments) is
applied and reviewed. **Q-003 update (2026-09-07):** the user resolved the `04_Chains`
content-strategy question by renaming that track to `03_Legacy_Chains/` (kept as
intentional legacy-API content, `4.0`-`4.2` only) and moving `4.3_Branching_Routing_
Merging_Chains.ipynb` — the one notebook in that folder already written in LCEL — into
`04_LCEL/` as `3.7_Branching_Routing_Merging_Chains.ipynb`, alongside its natural
siblings. Cross-references in `1.8`, `3.4`, and `4.2` were updated accordingly. Q-002
(formal `langchain-0x-contrast` tag vs. plain note, for the now-unambiguously-legacy
`03_Legacy_Chains/` content) remains open.

## Summary

| Verdict | Count |
| --- | --- |
| keep | 19 |
| keep (needs work) | 18 |
| consolidate → retire | 1 |
| retire (no replacement) | 0 |

Blocking questions: 0 — Q-001, Q-002, Q-003 all resolved (see [Open questions](#open-questions))

## Revision notes

Detailed narrative log — see [Changelog](#changelog) at the end of this file for the
one-line-per-date summary table.

- **2026-09-07 (correction):** This plan was independently generated by two parallel planner
  runs that both wrote to this file; the second run overwrote the first without knowledge of
  it, and their triage of NB-011/NB-014 disagreed (first run: retire NB-014 outright; second
  run's overwrite: keep both, no relationship). Resolved by a direct read of both notebooks:
  neither call was fully right — NB-014 does promise content (Prompt Templates, Output
  Parsers) it never delivers, but it also holds two code examples NB-011 lacks. Verdict
  changed to consolidate (DUP-001): port the unique HuggingFace content into NB-011, then
  retire NB-014. Counts above reflect this correction.
- **2026-09-07 (applied):** DUP-001 executed — the only item approved in
  `01_LangChain_Fundamentals_cleanup_decisions.md` round 1. Ported the `HuggingFacePipeline`
  local-inference walkthrough and raw `InferenceClient` streaming example into
  `2.3_LLM_vs_ChatModel.ipynb` (replacing its previously-commented-out local-pipeline
  placeholder cells with a live version, plus a new "Bonus" section), then retired
  `2.6_LangChain_LLM_Input_Output_Comprehensive.ipynb` to
  `archive/01_LangChain_Fundamentals/02_Inputs_Outputs_Prompts/` via `git mv` (row added to
  `archive/RETIRED_MANIFEST.md`). Reviewed by a fresh `notebook-review` subagent: **APPROVED**,
  round 1, 2 non-blocking nits (potential double-BOS token in the Gemma prompt; cosmetic
  import order). All FMT-###, MIG-###, other NB-### rows, and Q-001/Q-002 remain unruled and
  untouched — carried to the next round.
- **2026-09-07 (FMT pass applied):** 12 of the approved FMT-### rows executed
  (FMT-001–007, 009, 010, 013–017 — title/summary cells added, banner comments added to
  `3.3`/`3.4`, `1.6`'s mislabeled title cell corrected and its two empty trailing cells
  removed). FMT-008 rejected as moot (file retired). FMT-011/012/018 deferred (Q-002
  unanswered / requires notebook execution). All edits reviewed by fresh `notebook-review`
  subagents: `1.6`, `3.3`, `3.4` — **APPROVED**, 0 code-logic mismatches found in `3.3`/`3.4`'s
  55 banner-commented cells. Full validation script confirmed 0 syntax errors and correct
  title/summary structure across all 14 touched notebooks.
- **2026-09-07 (Q-003 recovered):** the user surfaced screenshots showing open-question text
  that did not match this file on disk. Investigation traced it to the original planner
  fork's analysis of `04_Chains` vs. `04_LCEL`/`06_Workflow_Patterns` overlap, which was
  silently dropped by the second fork's overwrite (see the first correction entry above) —
  this session's earlier conflict-resolution pass only checked the one disagreement it
  happened to notice (NB-014/DUP-001) and missed this second one. Independently re-verified
  against the actual notebook code (grepped `4.0`-`4.2` for LCEL vs. legacy-chain syntax)
  before restoring it as Q-003, rather than trusting either fork's or the user's wording
  as-is. Not yet answered.
- **2026-09-07 (Q-002 applied + folder renumbering + a self-inflicted bug fixed):**
  Q-002 answered (plain markdown note, see the row above) and applied to 5 notebooks.
  Mid-task, the on-disk folder names changed again outside this session's control —
  `04_Legacy_Chains/`→`03_Legacy_Chains/` and `03_LCEL/`→`04_LCEL/` — discovered via `git
  status` showing the git-tracked paths as deleted and the new names as untracked. Content
  was verified intact via git's rename detection (100% match) before proceeding; user
  confirmed to work against whatever is on disk. Separately, an editing mistake was caught
  and fixed: an insert-then-replace-by-stale-id sequence on `4.2_Advanced_Chains.ipynb`
  had overwritten its `MultiPromptChain`/`LLMRouterChain` construction-and-invoke code cell
  with duplicate Summary markdown text (cell type left as `code`) — restored the lost code
  from an earlier full read in this same conversation, removed the true duplicate, verified
  cell count, code-cell count, and zero syntax errors afterward. All cross-references to the
  old folder names were then swept and fixed repo-wide (`1.8`, `2.4`, `2.5`, `3.4`, `4.2`,
  `6.1`, plus a pre-existing unrelated dangling filename in `6.1`). Final validation: all 37
  notebooks in this folder parse with zero syntax errors.

## 1. Triage

One row per notebook. `Verdict` is a **proposal**; the decisions file overrides it.

### `01_Getting_Started/`

| ID | Notebook | Verdict | Evidence | Superseded by |
| --- | --- | --- | --- | --- |
| NB-001 | `1.0_LangChain_Introduction.ipynb` | keep (needs work) | No H1 title cell, no Summary cell (see FMT-001) | — |
| NB-002 | `1.1_Commercial_LLMs_Natively.ipynb` | keep | Inventory flagged `dead_end=3` ("1 abandonment marker" + "multiple fully commented-out cells"). Read in full: 27 cells, complete two-part OpenAI/Gemini walkthrough with proper title and Summary cells. The "scratch identifier" trigger is a reused generic variable name (`prompt`/`response`) across cells, not leftover scratch code; the "fully commented-out cells" are deliberate `# !pip install -qq openai` / "Method 1: alternative approach" reference blocks, consistent with the pattern in NB-003/NB-004/NB-033 | — |
| NB-003 | `1.2_Commercial_LLMs_with_LangChain.ipynb` | keep | `dead_end=1` from "multiple fully commented-out cells" — confirmed same intentional install/alt-method banner pattern as NB-002 | — |
| NB-004 | `1.3_Open_Source_LLMs_HF_Transformers.ipynb` | keep | Same intentional commented-cell pattern as NB-002/NB-003 | — |
| NB-005 | `1.4_Open_Source_LLMs_HF_Inference_Client.ipynb` | keep (needs work) | No H1 title cell (see FMT-002) | — |
| NB-006 | `1.5_Open_Source_LLMs_Groq_Cloud.ipynb` | keep | No flags | — |
| NB-007 | `1.6_Open_Source_LLMs_with_LangChain.ipynb` | keep (needs work) | Read in full: genuine content (HF local pipeline, HF Inference API, Groq — all through LangChain's `Chat*` wrappers), but markedly thinner than siblings — no title cell, no Summary, almost no explanatory markdown, and it ends in **two fully empty cells**. It's the only notebook in the folder covering LangChain-wrapped local/Groq LLM usage, so per the "never retire the only coverage" rule this is `keep (needs work)`, not retire (see FMT-003) | — |
| NB-008 | `1.8_Package_Split_and_Imports_LangChain_v1.ipynb` | keep | No flags | — |

### `02_Inputs_Outputs_Prompts/`

| ID | Notebook | Verdict | Evidence | Superseded by |
| --- | --- | --- | --- | --- |
| NB-009 | `2.1_LangChain_Inputs_and_Outputs.ipynb` | keep (needs work) | No Summary cell (see FMT-004) | — |
| NB-010 | `2.2_Prompt_Templates.ipynb` | keep | No flags | — |
| NB-011 | `2.3_LLM_vs_ChatModel.ipynb` | keep (needs work) | No Summary cell (see FMT-005). Target of DUP-001 — receives the unique HuggingFace content ported from NB-014 | — |
| NB-012 | `2.4_PromptTemplate_with_LangChain.ipynb` | keep (needs work) | No Summary cell; largest notebook in the folder (92 cells), clearly substantive (see FMT-006) | — |
| NB-013 | `2.5_Output_Parser.ipynb` | keep (needs work) | No Summary cell (see FMT-007) | — |
| NB-014 | `2.6_LangChain_LLM_Input_Output_Comprehensive.ipynb` | consolidate → retire | Verified by direct read (correcting a prior planner-run conflict — see Changelog). Its intro promises "Prompt Templates" and "Output Parsers" coverage that the notebook never delivers — it stops at conversational message-passing, identical territory to NB-011. Uses interactive `getpass()` for API keys where NB-011 and the rest of the folder use `.env`/`dotenv` — a step backward, not forward. Not a clean duplicate, though: it has content NB-011 lacks — a live `HuggingFacePipeline` local-inference walkthrough (cells 39-46) and a raw `huggingface_hub.InferenceClient` streaming example (cells 66-70). Per DUP-001: port those two pieces into NB-011, then retire this file | NB-011 (`2.3_LLM_vs_ChatModel.ipynb`) |

### `04_LCEL/`

| ID | Notebook | Verdict | Evidence | Superseded by |
| --- | --- | --- | --- | --- |
| NB-015 | `3.1_LCEL_Introduction.ipynb` | keep | No flags | — |
| NB-016 | `3.2_Runnables.ipynb` | keep | No flags | — |
| NB-017 | `3.3_LCEL_Deepdive.ipynb` | keep (needs work) | No title/no summary. Read in full: complete, coherent, externally-sourced (Udemy-linked) walkthrough of `\|` operator overloading → a hand-rolled `Runnable` → real LangChain `Runnable`s, ending in a working nested-chain invocation. Raw/terse style (few banner comments, minimal prose) relative to the rest of the folder, but not incomplete (see FMT-009) | — |
| NB-018 | `3.4_LCEL_and_Runnables.ipynb` | keep (needs work) | Same pattern as NB-017 — complete LCEL/RAG walkthrough ending in a working retrieval chain, no title/summary, terse style (see FMT-010) | — |
| NB-019 | `3.5_Chain_Migrations.ipynb` | keep | Migration scanner reported 4 `BLOCKING` findings for `LLMChain`/`RetrievalQA`. Read: both imports already point at `from langchain_classic...`, i.e. already correctly repointed per the "subject is the legacy API → repoint, don't rewrite" rule — the notebook's entire purpose is teaching the legacy→1.x chain migration. No rewrite needed; missing only the explicit "this is intentional legacy content" label a repo-native explainer would carry (see FMT-011, and Q-002 on whether formal tagging is required) | — |
| NB-020 | `3.6_Chain_Migration_Advanced.ipynb` | keep | Same situation as NB-019 — 2 `BLOCKING` findings, both already on `langchain_classic.chains.ConversationalRetrievalChain` (see FMT-012) | — |
| NB-024 | `3.7_Branching_Routing_Merging_Chains.ipynb` | keep (needs work) | **Moved here 2026-09-07** from `04_Chains/4.3_...` per the user's Q-003 resolution — this notebook was already written in current (non-legacy) LCEL branching/routing/merging patterns, so it belongs beside its LCEL siblings rather than in the renamed `03_Legacy_Chains/`. No migration findings. Summary cell added (FMT-016, applied) | — |

### `03_Legacy_Chains/` (renamed 2026-09-07, was `04_Chains/` — Q-003)

Kept as intentional legacy-`LLMChain`-family content per the user's Q-003 resolution.
`4.3` (LCEL-native) moved out to `04_LCEL/3.7_...` — see above.

| ID | Notebook | Verdict | Evidence | Superseded by |
| --- | --- | --- | --- | --- |
| NB-021 | `4.0_Basics_of_Chains.ipynb` | keep (needs work) | 6 `BLOCKING` findings, all on `langchain_classic.chains.{LLMChain,SimpleSequentialChain}` — already correctly repointed (confirmed by reading cell 1's imports), same legacy-teaching-subject situation as NB-019/NB-020. No title/no summary (see FMT-013) | — |
| NB-022 | `4.1_Chains_Basics_and_OutputParsers.ipynb` | keep (needs work) | 2 `BLOCKING` findings, `langchain_classic.chains.llm.LLMChain` — already repointed. No title/no summary (see FMT-014) | — |
| NB-023 | `4.2_Advanced_Chains.ipynb` | keep (needs work) | 9 `BLOCKING` + 4 `MODERNIZE` findings — `LLMChain`, `SequentialChain`, `MultiPromptChain`, `LLMRouterChain`, all already imported from `langchain_classic`. No title/no summary (see FMT-015) | — |

### `05_Summarization/`

| ID | Notebook | Verdict | Evidence | Superseded by |
| --- | --- | --- | --- | --- |
| NB-025 | `8.0_Summarization_Essentials.ipynb` | keep (needs work) | No title/no summary (see FMT-017). Filename numbering (`8.0`) doesn't match the folder's own `05_` prefix — cosmetic leftover from the source course's numbering, not a defect worth planning around | — |
| NB-026 | `8.2_Doc_Chains_to_LCEL_LangChain_v1.ipynb` | keep (needs work) | 2 `MODERNIZE` findings: imports `create_stuff_documents_chain` via the 0.3 bridge path rather than the current one (see MIG-001) | — |

### `06_Workflow_Patterns/`

| ID | Notebook | Verdict | Evidence | Superseded by |
| --- | --- | --- | --- | --- |
| NB-027 | `6.1_Prompt_Chaining.ipynb` | keep | No flags | — |
| NB-028 | `6.2_Routing.ipynb` | keep | No flags | — |
| NB-029 | `6.3_Parallelization.ipynb` | keep | No flags | — |
| NB-030 | `6.4_Orchestrator_Worker.ipynb` | keep | No flags | — |
| NB-031 | `6.5_Evaluator_Optimizer.ipynb` | keep | No flags | — |

### `07_LangChain_1x_Agents_and_Middleware/`

| ID | Notebook | Verdict | Evidence | Superseded by |
| --- | --- | --- | --- | --- |
| NB-032 | `7.0_LangChain_First_Agent.ipynb` | keep | Similarity pair with NB-034 (combined 0.81, driven by identical imports/symbols — both define `get_weather`). Read headings: this is "LangChain First Agent" (agent basics), NB-034 is "Tools — Letting the Model Call Code" (tools deep-dive). Sequential lesson pair reusing the same example tool, not a duplicate | — |
| NB-033 | `7.1_Model_Integration.ipynb` | keep | `dead_end=1` from "multiple fully commented-out cells" — same intentional pattern as NB-002/003/004 | — |
| NB-034 | `7.2_Tools.ipynb` | keep | See NB-032 | — |
| NB-035 | `7.3_Messages.ipynb` | keep | No flags | — |
| NB-036 | `7.4_Structured_Output.ipynb` | keep | No flags | — |
| NB-037 | `7.5_Middleware.ipynb` | keep (needs work) | `executed_out_of_order=1` (see FMT-018); 2 `MODERNIZE` findings for `config['configurable']` context usage (see MIG-002) | — |
| NB-038 | `7.6_LangGraph_Interrupt_Primitive.ipynb` | keep | No flags | — |

Evidence is facts from the inventory and from reading the notebook. Not adjectives.

## 2. Consolidations

| ID | Keep | Retire | Port before retiring | Effort |
| --- | --- | --- | --- | --- |
| DUP-001 | NB-011 `2.3_LLM_vs_ChatModel.ipynb` | NB-014 `2.6_LangChain_LLM_Input_Output_Comprehensive.ipynb` | The `HuggingFacePipeline` local-inference walkthrough (NB-014 cells 39-46) and the raw `huggingface_hub.InferenceClient` streaming example (cells 66-70) — neither appears in NB-011, and NB-014's own advertised "Prompt Templates"/"Output Parsers" sections never materialize, so nothing else is worth carrying over | S |

Of the other 2 similarity pairs the scanner reported, both were read and resolved as
sequential lessons or differently-scoped notebooks reusing common imports/example code, not
duplicate coverage — see NB-032/NB-034 above.

## 3. Library migrations

| ID | Notebook | Library | Old API | New API | Effort | Note |
| --- | --- | --- | --- | --- | --- | --- |
| MIG-001 | ~~`05_Summarization/8.2_Doc_Chains_to_LCEL_LangChain_v1.ipynb` (NB-026)~~ | ~~LangChain~~ | ~~`from langchain.chains.combine_documents import create_stuff_documents_chain`~~ | — | — | **Invalid, 2026-09-07** — checked the full notebook cell-by-cell, this import string appears nowhere in it. The notebook's actual `load_summarize_chain` import is already correctly repointed to `langchain_classic.chains.summarize` (cell 8); its one legacy-API cell (cell 5) is explicitly marked "does not run on 1.x — contrast only" and is not to be touched. No action taken. |
| MIG-002 | ~~`07_LangChain_1x_Agents_and_Middleware/7.5_Middleware.ipynb` (NB-037)~~ | ~~LangGraph~~ | ~~`state.config['configurable']['thread_id']` used for app-level context~~ | — | — | **Invalid, 2026-09-07** — checked all 8 occurrences of `config["configurable"]["thread_id"]`; every one is the checkpointer's own thread-identification key (the correct, current LangGraph pattern for `agent.invoke(..., config=config)`), not app-level data. By the row's own original rule ("`Runtime[ContextSchema]` for anything *beyond* the checkpointer's own keys"), `thread_id` itself was never something to migrate. No action taken. |

`NB-019/020/021/022/023`'s `LLMChain`/`RetrievalQA`/etc. imports are **not** migration items —
they already point at `langchain_classic`, which is the correct, current way to keep teaching
a legacy API on purpose. See [Not planned](#6-not-planned).

## 4. Formatting

| ID | Notebook | Missing / wrong |
| --- | --- | --- |
| FMT-001 | `1.0_LangChain_Introduction.ipynb` (NB-001) | No `# Title` cell, no Summary cell |
| FMT-002 | `1.4_Open_Source_LLMs_HF_Inference_Client.ipynb` (NB-005) | No `# Title` cell |
| FMT-003 | `1.6_Open_Source_LLMs_with_LangChain.ipynb` (NB-007) | No `# Title` cell, no Summary cell, two trailing empty cells, minimal explanatory markdown compared to sibling notebooks |
| FMT-004 | `2.1_LangChain_Inputs_and_Outputs.ipynb` (NB-009) | No Summary cell |
| FMT-005 | `2.3_LLM_vs_ChatModel.ipynb` (NB-011) | No Summary cell |
| FMT-006 | `2.4_PromptTemplate_with_LangChain.ipynb` (NB-012) | No Summary cell |
| FMT-007 | `2.5_Output_Parser.ipynb` (NB-013) | No Summary cell |
| FMT-008 | `2.6_LangChain_LLM_Input_Output_Comprehensive.ipynb` (NB-014) | Moot — file is retired per DUP-001, not formatted in place |
| FMT-009 | `3.3_LCEL_Deepdive.ipynb` (NB-017) | No `# Title` cell, no Summary cell, no code-cell banner comments |
| FMT-010 | `3.4_LCEL_and_Runnables.ipynb` (NB-018) | No `# Title` cell, no Summary cell, no code-cell banner comments |
| FMT-011 | `3.5_Chain_Migrations.ipynb` (NB-019) | **Applied 2026-09-07** — plain markdown note added at the top, per Q-002's answer |
| FMT-012 | `3.6_Chain_Migration_Advanced.ipynb` (NB-020) | **Applied 2026-09-07** — same |
| FMT-013 | `03_Legacy_Chains/4.0_Basics_of_Chains.ipynb` (NB-021) | Title/Summary cells — **applied**. Legacy-label gap remains (blocked on Q-002) |
| FMT-014 | `03_Legacy_Chains/4.1_Chains_Basics_and_OutputParsers.ipynb` (NB-022) | Title/Summary cells — **applied**. Legacy-label gap remains (blocked on Q-002) |
| FMT-015 | `03_Legacy_Chains/4.2_Advanced_Chains.ipynb` (NB-023) | Title/Summary cells — **applied**. Legacy-label gap remains (blocked on Q-002) |
| FMT-016 | `04_LCEL/3.7_Branching_Routing_Merging_Chains.ipynb` (NB-024, moved from `04_Chains/4.3_...` per Q-003) | Summary cell — **applied** |
| FMT-017 | `8.0_Summarization_Essentials.ipynb` (NB-025) | No `# Title` cell, no Summary cell |
| FMT-018 | `7.5_Middleware.ipynb` (NB-037) | **Applied 2026-09-07** — re-executed top to bottom (`jupyter nbconvert --execute`, kernel `python3`). `execution_count` now 1→25 monotonic across all 25 code cells, zero errors, all three HITL scenarios (approve/reject/edit) and both summarization triggers ran to completion with sensible output. Outputs left in place for review; will be nulled/stripped automatically by this repo's `nbstripout` pre-commit hook at commit time. |

## 5. Open questions

Things a human has to answer. Each blocks only the items listed.

| ID | Question | Blocks |
| --- | --- | --- |
| Q-001 | **Resolved as moot, 2026-09-07.** Originally: does this folder overlap/need merging with the roadmap's `02_LangChain_Fundamentals_and_Prompting/LangChain_Fundamentals/` track? On investigation, that track no longer exists — the repo's top-level phase structure was renumbered 0-16 in commit `30ddca0`, and `01_LangChain_Fundamentals` is already the current Phase 1 folder. `CLAUDE.md`/`NOTEBOOK_INDEX.md` still describe the old 13-phase, 1-indexed layout and are stale (flagged separately, not part of this cleanup). User confirmed the renumbering is intentional/final. No merge or move needed. | none (resolved) |
| Q-002 | **Answered 2026-09-07.** Five notebooks (NB-019, NB-020, NB-021, NB-022, NB-023) intentionally teach the legacy LangChain chain API via `langchain_classic`. The existing `langchain-0x-contrast` cell-metadata tag (used in `1.8`/`8.2`) marks cells that are *broken* on 1.x, shown purely for contrast — semantically a poor fit here, since these cells run fine via `langchain_classic`, just on a retired API. User chose a plain visible markdown note instead of stretching the existing tag or inventing a new one. Applied: a note cell inserted at the top of `3.5`, `3.6`, `4.0`, `4.1`, `4.2` explaining the intentional legacy-API use and pointing to the LCEL equivalents. | FMT-011, FMT-012, FMT-013, FMT-014, FMT-015 — all applied |
| Q-003 | **Answered 2026-09-07.** Restored after being dropped by an earlier planner-run conflict, independently re-verified against the actual notebook code (confirmed `4.0`-`4.2` use zero LCEL syntax, entirely `LLMChain`-family APIs). User's decision: keep `4.0`-`4.2` as intentional legacy-API content (folded into Q-002's tagging question, still open) in a folder renamed `04_Chains/` → `03_Legacy_Chains/` to make that intent explicit; move `4.3_Branching_Routing_Merging_Chains.ipynb` (the one notebook in that folder already on LCEL) into `04_LCEL/` as `3.7_Branching_Routing_Merging_Chains.ipynb`, beside its true siblings. Executed via `git mv` (history preserved); cross-references updated in `1.8_Package_Split_and_Imports_LangChain_v1.ipynb`, `3.4_LCEL_and_Runnables.ipynb`, and `4.2_Advanced_Chains.ipynb`. | Resolved — see [Triage](#1-triage) and FMT-013–016 |

## 6. Not planned

- **`LLMChain`/`RetrievalQA`/`SequentialChain`/`MultiPromptChain`/`ConversationalRetrievalChain` imports in NB-019 through NB-023** — all already point at `langchain_classic`, which is the correct current form for a notebook whose subject is the legacy chain API. No rewrite planned; see MIG note above and Q-002 for the labelling question.
- **2 similarity pairs read and confirmed not duplicates**: NB-011/NB-014 (different scope — comparison vs. comprehensive guide) and NB-032/NB-034 (sequential agent-basics/tools-deep-dive lessons sharing an example tool).
- **"Fully commented-out cells" flagged on NB-002, NB-003, NB-004, NB-033** — confirmed intentional `pip install` / "alternative approach" reference blocks in every case read, a consistent authoring pattern across the folder. No action.
- **`requirements.txt` / `pyproject.toml`** — not touched, per skill rules. This folder was not checked against the root env's pins; if it has its own dependency file it wasn't inventoried here.
- **Filename numbering oddities** (`8.0`/`8.2` inside a `05_` folder, non-contiguous `1.x`/`2.x`/`3.x` sequences) — cosmetic leftovers from the source course, not planned for renumbering; would only matter if/when Q-001 is resolved.
- **Folder placement in the roadmap** — out of this skill's scope; see Q-001.

## Changelog

| Date | Change |
| --- | --- |
| 2026-09-07 | Initial plan, 38 notebooks |
| 2026-09-07 | Corrected NB-011/NB-014 verdict after a two-fork write conflict; changed to consolidate (DUP-001) |
| 2026-09-07 | DUP-001 applied and reviewed (APPROVED) |
| 2026-09-07 | 12 FMT items applied across 14 notebooks and reviewed (APPROVED) |
| 2026-09-07 | Q-003 recovered (dropped by the same fork conflict) and independently re-verified |
| 2026-09-07 | Q-001 resolved as moot — folder already placed correctly, roadmap was renumbered in `30ddca0` |
| 2026-09-07 | Q-003 answered — `04_Chains/` renamed to `03_Legacy_Chains/`; `4.3_...` moved to `04_LCEL/3.7_...`; cross-references updated; plan re-synced to new paths |
| 2026-09-07 | MIG-001 and MIG-002 investigated on user request — both invalidated (cited code doesn't exist / cited usage is already correct); marked invalid, no edits made |
| 2026-09-07 | FMT-018 applied — `7.5_Middleware.ipynb` re-executed top to bottom, execution counts now monotonic, zero errors |
| 2026-09-07 | Q-002 answered and applied — plain markdown note added to `3.5`, `3.6`, `4.0`, `4.1`, `4.2`; FMT-011/012 applied |
| 2026-09-07 | Folders renumbered again outside this session (`04_Legacy_Chains`→`03_Legacy_Chains`, `03_LCEL`→`04_LCEL`); content verified intact via git rename detection; a self-inflicted content-loss bug in `4.2` (stale-cell-id edit) found and fixed; all cross-references repo-wide swept and corrected; 0 blocking questions remain |
