# Additional Apps in This Phase

**`building-adaptive-rag/`** — ✅ Built. A standalone deployable adaptive-RAG app (own `src/`, `tests/`, `main.py`, `requirements.txt`) merged from `RAG_Demystified`.

**`mcp_a2a_agentic_rag/`** — ✅ Built. An agentic RAG app built on MCP and A2A protocols — kept here (RAG-first) rather than split into Phase 9's Agent Protocols, per user decision, even though it touches that phase's topic too.

Both stayed as RAG-first apps in this phase rather than moving to Phase 13 Projects, per user decision — Phase 13 is reserved for the more general framework capstones.
